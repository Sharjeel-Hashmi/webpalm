!(function (o) {
  const e = "has-child-dropdown-show";
  var t;
  (o.Dropdown.prototype.toggle =
    ((t = o.Dropdown.prototype.toggle),
    function () {
      document.querySelectorAll("." + e).forEach(function (o) {
        o.classList.remove(e);
      });
      let o = this._element
        .closest(".dropdown")
        .parentNode.closest(".dropdown");
      for (; o && o !== document; o = o.parentNode.closest(".dropdown"))
        o.classList.add(e);
      return t.call(this);
    })),
    document.querySelectorAll(".dropdown").forEach(function (o) {
      o.addEventListener("hide.bs.dropdown", function (o) {
        this.classList.contains(e) &&
          (this.classList.remove(e), o.preventDefault()),
          o.stopPropagation();
      });
    }),
    document
      .querySelectorAll(".dropdown-hover, .dropdown-hover-all .dropdown")
      .forEach(function (t) {
        t.addEventListener("mouseenter", function (n) {
          let r = n.target.querySelector(':scope>[data-bs-toggle="dropdown"]');
          r.classList.contains("show") ||
            (o.Dropdown.getOrCreateInstance(r).toggle(),
            t.classList.add(e),
            o.Dropdown.clearMenus());
        }),
          t.addEventListener("mouseleave", function (e) {
            let t = e.target.querySelector(
              ':scope>[data-bs-toggle="dropdown"]'
            );
            t.classList.contains("show") &&
              o.Dropdown.getOrCreateInstance(t).toggle();
          });
      });
})(bootstrap);
// !(function (o) {
//   const e = "has-child-dropdown-show";
//   var t;

//   o.Dropdown.prototype.toggle = (function (originalToggle) {
//     return function () {
//       // Remove class from all previously opened dropdowns
//       document.querySelectorAll("." + e).forEach(function (dropdown) {
//         dropdown.classList.remove(e);
//       });

//       // Ensure _element exists before proceeding
//       let o = this._element?.closest(".dropdown");
//       if (o && o.parentNode) {
//         o = o.parentNode.closest(".dropdown");
//       }

//       // Iterate through ancestors safely
//       while (o && o !== document) {
//         o.classList.add(e);
//         o = o.parentNode ? o.parentNode.closest(".dropdown") : null;
//       }

//       return originalToggle.call(this);
//     };
//   })(o.Dropdown.prototype.toggle);

//   // Add event listener to remove class when dropdown is hidden
//   document.querySelectorAll(".dropdown").forEach(function (dropdown) {
//     dropdown.addEventListener("hide.bs.dropdown", function (event) {
//       if (this.classList.contains(e)) {
//         this.classList.remove(e);
//         event.preventDefault();
//       }
//       event.stopPropagation();
//     });
//   });

//   // Handle hover effect on dropdowns
//   document
//     .querySelectorAll(".dropdown-hover, .dropdown-hover-all .dropdown")
//     .forEach(function (dropdownElement) {
//       dropdownElement.addEventListener("mouseenter", function (event) {
//         let trigger = event.target.querySelector(
//           ':scope>[data-bs-toggle="dropdown"]'
//         );
//         if (trigger && !trigger.classList.contains("show")) {
//           o.Dropdown.getOrCreateInstance(trigger).toggle();
//           dropdownElement.classList.add(e);
//           o.Dropdown.clearMenus();
//         }
//       });

//       dropdownElement.addEventListener("mouseleave", function (event) {
//         let trigger = event.target.querySelector(
//           ':scope>[data-bs-toggle="dropdown"]'
//         );
//         if (trigger && trigger.classList.contains("show")) {
//           o.Dropdown.getOrCreateInstance(trigger).toggle();
//         }
//       });
//     });
// })(bootstrap);
